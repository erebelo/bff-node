{
  "name": "bff-node-poc",
  "version": "1.0.0",
  "description": "BFF POC using nodeJS & express",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start:dev": "set NODE_ENV=dev && node --watch server.js",
    "start:prod": "set NODE_ENV=prod && node --watch server.js",
    "debug": "ndb server.js"
  },
  "author": "Eduardo Rebelo",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.4.0",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "morgan": "^1.10.0",
    "yup": "^1.2.0"
  },
  "devDependencies": {
    "ndb": "^1.1.5"
  }
}
